{
  programs.git = {
    enable = true;
    # userName = "[Bot]CookieCutterGames"; Right now i dont know why `programs.git.userName' does not exist.
    # userEmail = "contact@gamescutter.com"; Right now i dont know why `programs.git.userEmail' does not exist.
  };
}